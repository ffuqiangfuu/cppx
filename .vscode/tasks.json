{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file", // 标签更新为 g++
            "command": "D:\\DevelopTool\\msys64\\ucrt64\\bin\\g++.exe", // <--- 1. 修改为 g++.exe C++编译器
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}", // <--- 2. 使用 ${file} 来编译当前打开的文件
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe" // <--- 3. (推荐) 让输出的exe和你的文件名相同
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger for C++." // 细节描述更新
        }
    ],
    "version": "2.0.0"
}